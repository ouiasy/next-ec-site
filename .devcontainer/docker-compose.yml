services:
  app:
    image: my-next-node:v1
    build:
      context: .
      dockerfile: Dockerfile.node
    depends_on:
      - db

  db:
    image: my-postgres:v1
    build:
      context: .
      dockerfile: Dockerfile.db
    environment:
      POSTGRES_USER: "appuser"
      POSTGRES_PASSWORD: "my_password"
      POSTGRES_DB: "ecsite"
    volumes:
      - postgres-for-ecsite:/var/lib/postgres/data

volumes:
  postgres-for-ecsite: